Профилирование рекурсии.


from memory_profiler import profile
'''Подсчитаем сумму цифр натурального числа 

@profile
def func(n):
    if n < 10:
        return n
    return n % 10 + func(n-1)



print(func(25))'''
#При профилировании рекурсии заметил, что профилировщик запускает функцию несколько раз для того
# что бы выявить интервал выборки памяти, что позволит ему получить достаточно выборок для точного
#считывания. В данном случае наверное лучше использовать 'memory_usage' с уменьшением интервала.